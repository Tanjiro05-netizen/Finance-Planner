# Backend Override

- Backend implementation begins in Phase 4.
- Keep Plaid secrets server-side only and commit `.env.example`, never real `.env` files.
- Use TypeScript strict mode, Express, Prisma, zod, pino, and vitest when the backend phase starts.
